from config import start as main # Importation of the "engine purpose" function

if __name__ == "__main__":  # Starts the "app", if the main file "main.py" is run
    main() # Function called. Application Starts.